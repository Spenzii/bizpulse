<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    /**
     * Run the migrations.
     */
    public function up(): void
    {
        Schema::create('companies', function (Blueprint $table) {
            $table->id();
            $table->string('name');
            $table->string('slug')->unique();
            $table->string('subscription_plan')->default('Starter'); // Starter, Business, Professional, Enterprise
            $table->string('subscription_status')->default('Trial'); // Trial, Active, Overdue, Suspended, Cancelled, Expired
            $table->timestamp('trial_ends_at')->nullable();
            $table->boolean('setup_fee_paid')->default(false);
            $table->timestamps();
        });
    }

    /**
     * Reverse the migrations.
     */
    public function down(): void
    {
        Schema::dropIfExists('companies');
    }
};
